# URL del backend (la API). En local no hace falta (usa localhost por defecto).
# En producción (Render) poné acá la URL pública de tu backend, terminada en /api
# Ejemplo: https://silvia-backend.onrender.com/api
VITE_API_URL=
